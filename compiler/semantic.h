#pragma once 

#include <structs/tree.h>
#include <structs/hash.h>


/**
 * Semantic Checker Does the following
 * Type Check (do not allow any mismatch or promotion)\
 * Label check (all variables are declared and initialized)
 * Flow control check 
 */

/**
 * Errors the semantic checker should do
 * Variable redeclaration
 * Local objects not defined outside their scope
 * function missing return statement
 * function passed wrong number/type of arguments
 * function updates non-local variables
 * procedure has no return values
 * procedure passed wrong number/type of arguments
 * Array index out of bounds
 * Array index not an integer
 * Array index not a constant
 * Array index not a variable
 */




